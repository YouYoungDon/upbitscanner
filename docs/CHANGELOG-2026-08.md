# 변경 이력 — 2026년 8월

## 1. 주간분석 측정·검증 체계 개선 5종 (2026-08-01)

8/1 수동 주간분석에서 발견한 결함 일괄 수정. 설계:
`docs/superpowers/specs/2026-08-01-weekly-analysis-improvements-design.md`.
커밋 `0b36cdf`, `dcc834f`, `8520847`. 테스트 397 → 407개.

### 가중치 학습 +1일 확정종가 전환 (M7 백로그 해소)

- 기존: 신호가 대비 **분석 시점 현재가** 판정 — 6일 전 픽과 3시간 전 픽이
  같은 잣대(mixed-horizon). 감사 백로그 M7이 지적한 구조 결함.
- 변경: 스캔일 D0+1 **확정 일봉 종가** 판정 (`lib/weekly.mjs::judgeAtHorizon`).
  `signal-weights.meta.json`의 `learnedUntil` 가드로 --force 재실행·중복
  실행 시 이중 EWM 학습 원천 차단 (06-14 3중 조정 사고 재발 방지).
- 표시용 주간 통계는 최근 7일 전체(+1일 판정) — 재실행에도 리포트 보존.
- **교정 효과 실증**: 같은 주 데이터에서 역삼중바닥 적중률 0%(혼재 잣대)
  → **65%**(확정 +1일). 혼재 잣대가 신호 평가를 심각하게 왜곡하고 있었음.

### 헤드라인 매수/매도 분리

- `sideStats` 추가. "전체 62.8%"(매도 물량 착시) → "매수 18% · 매도 74%".

### 시간별 적중률 +7일 확보 + 캔들 캐시

- 조회 14일로 확장 → +7일 창 최초 산출(30.6%). 픽당 API 1콜(~900콜,
  100초)을 마켓별 일봉 캐시 1콜(~255콜, 36초)로 통합. 모멘텀 검증 공유.

### 🎯전략 SL/TP 자동 채점

- `lib/strategy.mjs::scoreStrategyOutcome`: 진입가 기준 D+1..D+holdMax
  확정봉 순회(SL 우선·결손일 스킵). 09:10 스코어카드 배치가
  `strategyOutcome` 기록, `/api/scorecard`에 전략 요약(승률·SL/TP/시간·
  보유중) + UI 카드·행 배지.
- 백필 결과(라이브 1주차): 6건 — SL 2(아더 7/29·소닉SVM), 보유 4
  (바운스토큰·API3·아더 7/31·칠리즈 8/1).

### ⚠️추격주의 감점 ×0.8 승격

- 라이브 실증(태그 19건, +1일 평균 -8.64% vs 전체 -3.59%)으로 표시 전용
  → 점수 ×0.8. BUY_THRESHOLD 미달 추격 픽은 목록 제외(의도된 효과).
  2026-07-25 스펙에 개정 각주.

## 2. 코드리뷰 반영 — CONFIRMED 11·PLAUSIBLE 3 전부 수정 (2026-08-01)

§1 배포 직후 /code-review(8앵글 파인더 → 1-vote 검증). 테스트 407 → 411개.

**learnedUntil 워터마크 3중 누수 (최심각)**
- 워터마크가 스캔 날짜만으로 전진해 fetch 실패·저유동 무거래일·14일 초과
  공백에서 영구 미학습으로 반전되던 문제를 근본 수정:
  ① 전진 기준을 "실제 판정 성공한 픽의 최신 ts"로 변경
  ② 판정 커버리지 80% 미만이면 가중치 갱신·전진 통째 보류(다음 실행 재시도)
  ③ `confirmedOhlcvAsOf`(날짜 인지 확정봉 — 당일 봉만 제거) 신설, 주간분석·
    스코어카드 적용: 당일 무거래 저유동 마켓의 어제 확정봉 보존
  ④ learnedUntil 공백만큼 조회 창 동적 확장(상한 60일) + 초과 시 경고
  ⑤ meta 파일 소실 시 주간 엔트리의 learned.learnedUntil로 자가 복구
- 스모크 테스트: 같은 날 재실행에서 학습 0건·워터마크 불변 확인(이전
  코드는 재학습했을 상황).

**추격 감점 재배치**: 보너스 가산 뒤 → 다른 배수 감점과 같은 위치(가산 전).
🚀Pump Start 발화 픽은 면제(급등 진입이 목적인 신호까지 과세하지 않음).

**측정·표시 정합**
- 가중치 변화 '이유'를 실제 학습 입력(learnStats) 기준으로 (표시 창 혼용 제거)
- 판정 가능 레코드 0건이면 주간 엔트리 저장 생략(가짜 0% 주간 방지) +
  UI 헤드라인 0/0건은 '-' 표시
- 모멘텀 '전체' 게이지 7일 창 복원(14일 확장은 timed에만 적용)
- `/api/verify` history에 per-entry `horizonMode` 노출(방법론 전환점 구분)
- 전략 요약에 noData 필드 추가(합계=n 정합) + 단일 순회 집계

**전략 채점 견고화**: D+holdMax 봉 결손(거래정지) 시 유예 3일 내 첫 봉
종가로 시간청산 — 백테스트(인덱스 기반)와의 괴리 축소. `resolveExit`
공유 코어로 simulateTrade·scoreStrategyOutcome의 청산 규칙 단일화.

**정리**: timedHitRates가 judgeHit 재사용, statsWithReturns 헬퍼로
표시/학습 집계 중복 제거.

REFUTED 2건(기각): 실패 경로 sleep 생략(→ get() 내부 백오프가 이미 더
느리게 페이싱), 마이그레이션 이중학습(→ 저널에 문서화된 1회성 교정).
