# 변경 이력 — 2026년 8월

## 1. 주간분석 측정·검증 체계 개선 5종 (2026-08-01)

8/1 수동 주간분석에서 발견한 결함 일괄 수정. 설계:
`docs/superpowers/specs/2026-08-01-weekly-analysis-improvements-design.md`.
커밋 `0b36cdf`, `dcc834f`, `8520847`. 테스트 397 → 407개.

### 가중치 학습 +1일 확정종가 전환 (M7 백로그 해소)

- 기존: 신호가 대비 **분석 시점 현재가** 판정 — 6일 전 픽과 3시간 전 픽이
  같은 잣대(mixed-horizon). 감사 백로그 M7이 지적한 구조 결함.
- 변경: 스캔일 D0+1 **확정 일봉 종가** 판정 (`lib/weekly.mjs::judgeAtHorizon`).
  `signal-weights.meta.json`의 `learnedUntil` 가드로 --force 재실행·중복
  실행 시 이중 EWM 학습 원천 차단 (06-14 3중 조정 사고 재발 방지).
- 표시용 주간 통계는 최근 7일 전체(+1일 판정) — 재실행에도 리포트 보존.
- **교정 효과 실증**: 같은 주 데이터에서 역삼중바닥 적중률 0%(혼재 잣대)
  → **65%**(확정 +1일). 혼재 잣대가 신호 평가를 심각하게 왜곡하고 있었음.

### 헤드라인 매수/매도 분리

- `sideStats` 추가. "전체 62.8%"(매도 물량 착시) → "매수 18% · 매도 74%".

### 시간별 적중률 +7일 확보 + 캔들 캐시

- 조회 14일로 확장 → +7일 창 최초 산출(30.6%). 픽당 API 1콜(~900콜,
  100초)을 마켓별 일봉 캐시 1콜(~255콜, 36초)로 통합. 모멘텀 검증 공유.

### 🎯전략 SL/TP 자동 채점

- `lib/strategy.mjs::scoreStrategyOutcome`: 진입가 기준 D+1..D+holdMax
  확정봉 순회(SL 우선·결손일 스킵). 09:10 스코어카드 배치가
  `strategyOutcome` 기록, `/api/scorecard`에 전략 요약(승률·SL/TP/시간·
  보유중) + UI 카드·행 배지.
- 백필 결과(라이브 1주차): 6건 — SL 2(아더 7/29·소닉SVM), 보유 4
  (바운스토큰·API3·아더 7/31·칠리즈 8/1).

### ⚠️추격주의 감점 ×0.8 승격

- 라이브 실증(태그 19건, +1일 평균 -8.64% vs 전체 -3.59%)으로 표시 전용
  → 점수 ×0.8. BUY_THRESHOLD 미달 추격 픽은 목록 제외(의도된 효과).
  2026-07-25 스펙에 개정 각주.
